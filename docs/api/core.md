# Module: `nim_esphome`

The main library entrypoint for `nim-esphome`. Re-exports all submodules (`api`, `entities`, `gpio`, `i2c`, `preferences`, `dsp`) and provides core lifecycle templates and C++ export macros.

::: nim_esphome
